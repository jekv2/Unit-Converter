Just a Unit Convert, I instructed chagpt, to make a ps1 script for a unit converter and working with chatgpt with bugs with it, then converted it to a UnitConverter.exe. Using Invoke-PS2EXE. The Unit Converter is for speed units, like Example: 1 Gbps to MB/s, all speed units. Nifty. I do a lot of conversions and could not find a converter, this is perfect.

Create a .ps1 file called unit_converter.ps1, add the following below and save it.
You can find a script to add right click run in powershell as admin or not admin in right clicking.
Right click run in powershell.
And/or use Invoke-PS2EXE in powershell to create a .exe. All files will be attached.
